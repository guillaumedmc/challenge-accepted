<%= simple_form_for([ challenge, comment ]) do |form| %>
  <%= form.input :content, as: :text, label: 'Add a comment',required: true, class: 'form-control' %>
  <%= form.button :submit, class: "btn btn-primary"%>
<% end %>
